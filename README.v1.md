# Bickford

Execution-grade persistence engine.

## Structure

- `canon/` — Immutable rule system
- `packages/` — Core logic (types, authority, ledger, optr)
- `apps/web/` — Next.js execution interface

## Quick Start

```bash
npm install
npm run build
npm run dev
```

Visit http://localhost:3000
