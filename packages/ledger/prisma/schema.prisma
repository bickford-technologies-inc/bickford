// Minimal stub Prisma schema to unblock Vercel build

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    // url property moved to prisma/prisma.config.ts for Prisma 7+
}

model Dummy {
    id    Int     @id @default(autoincrement())
    value String?
}
