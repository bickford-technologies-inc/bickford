{
  "name": "bickford-mobile-expo",
  "version": "0.1.0",
  "private": true,
  "description": "Bickford Mobile (Expo/EAS) native wrapper",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "icon:generate": "node scripts/generate-icon.mjs",
    "postinstall": "node -e \"if (process.env.CI !== 'true' && process.env.RAILWAY !== 'true') { require('node:child_process').execSync('npm run icon:generate', {stdio: 'inherit'}) } else { console.log('Skipping icon generation in CI/Railway') }\"",
    "start": "expo start",
    "ios": "expo run:ios",
    "android": "expo run:android",
    "prebuild": "expo prebuild --clean",
    "eas:build:ios": "eas build --platform ios --profile production",
    "eas:submit:ios": "eas submit --platform ios --profile production",
    "lint": "pnpm exec pnpm exec tsc -p tsconfig.json"
  },
  "dependencies": {
    "expo": "~54.0.30"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
