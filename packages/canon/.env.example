# Bickford API env example
# Copy to: packages/bickford/.env

# Demo/dev convenience (no auth). For real deployments, set AUTH_MODE=jwt and configure JWT_*.
AUTH_MODE=none

# JWT (only used if AUTH_MODE=jwt)
JWT_SECRET=super_long_secret_32_chars_min
JWT_ISSUER=bickford
JWT_AUDIENCE=bickford-canon

# Optional infra (not required for the basic demo)
REDIS_URL=redis://localhost:6379
REDIS_PREFIX=bickford:canon

DATABASE_URL=postgresql://user:pass@host:5432/dbname
PG_SSL=true

# Authorization Mode
# - permissive: Always allow (default); LLM provides summaries
# - audit: Always allow; log when LLM would deny
# - enforce: Enforce ALLOW/DENY when LLM is configured
AUTH_DECISION_MODE=permissive

# OpenAI (optional; enables LLM-backed chat)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TIMEOUT_MS=15000
