# Datalake Structure

- workflows/ — canonical folders for each workflow (by keyword)
- ledger.jsonl — append-only log of decisions executed
- messages.jsonl — chat message log (with keywords)
- knowledge-schema.json — all compound structural knowledge
- agents.json — agent registry, import/export, last-action
- interchange/ — import/export actions per agent
