[build]
builder = "nixpacks"
buildCommand = "bun install && bun run build"

[deploy]
startCommand = "bun run bickford-intelligence/server.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "always"
