// Minimal stub Prisma schema to unblock build

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    // url property moved to prisma/prisma.config.ts for Prisma 5+
}

model Dummy {
    id    Int     @id @default(autoincrement())
    value String?
}
