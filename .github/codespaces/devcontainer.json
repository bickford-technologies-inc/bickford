{
  "name": "Bickford (Codespaces Optimized)",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "postCreateCommand": "corepack enable && pnpm install && pnpm run setup && echo '✅ Codespace ready! Run: pnpm run start'",
  "remoteEnv": {
    "OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY:}"
  },
  "forwardPorts": [3000, 5173],
  "portsAttributes": {
    "3000": {"label": "API", "onAutoForward": "notify"},
    "5173": {"label": "Web UI", "onAutoForward": "openBrowser"}
  }
}
