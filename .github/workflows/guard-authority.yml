name: Guard — Execution Requires Authority

on:
  pull_request:
  push:
    branches: [main]

jobs:
  authority-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Enforce authority usage
        run: |
          pnpm install --frozen-lockfile
          pnpm tsx scripts/check-authority-enforcement.ts
